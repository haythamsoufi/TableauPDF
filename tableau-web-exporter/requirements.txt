Flask>=2.0
pandas>=1.3
openpyxl>=3.0 # Needed by pandas for .xlsx files
tableauserverclient>=0.18 # Or the version you need
requests>=2.25
python-dotenv>=0.19 # For managing config like API keys securely
celery>=5.0 # For background tasks (setup required later)
redis>=4.0 # Common message broker for Celery (or RabbitMQ)